version: '3.9'

services:
  app:
    build:
      context: ../../../src
      dockerfile: ./deployment/test/Dockerfile
    volumes:
      - ../../../src/psmproject:/psmproject
    ports:
      - "8000:8000"
    # env_file:
    #   - ./.env
